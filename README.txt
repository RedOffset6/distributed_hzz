This application performs the hzz analysis.

The container images can be built using the following command:

. ./build_project

The application can be started by running the following command:

docker compose up